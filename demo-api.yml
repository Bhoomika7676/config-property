server:
  port: 8084

spring:
  boot:
    admin:
      client:
        url: http://localhost:1111


management:
  endpoints:
    web:
      exposure:
        include: "*"

message: "this is a message from demo config server"
    
